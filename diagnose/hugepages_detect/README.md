### User Guide for Hugepages Status Detection

#### 开启大页分配服务(hugetlb_gigantic_pages.service)时

```
     output:
     宿主机各节点大页数量
     与Hugepages_node.conf文件中预配置的各节点大页数量比对情况
     各运行中云主机占用大页数量
```

#### 未开启大页(hugetlb_gigantic_pages.service)时

```
     output：
     宿主机各节点大页数量
     各运行中云主机占用大页数量
```

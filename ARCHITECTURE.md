# 系统架构文档

## 整体架构
VMAnalyzer采用模块化设计，主要分为以下几个核心模块：

### 架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         VM Analyzer Agent                               │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     用户接口层 (CLI/API)                         │   │
│  │                      agent/main.py                               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                  │                                      │
│  ┌───────────────────────────────┼───────────────────────────────┐     │
│  │                               ▼                               │     │
│  │  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐       │     │
│  │  │   采集层    │───▶│   存储层    │───▶│   分析层    │       │     │
│  │  │  gather/    │    │agent/storage│    │agent/analyze│       │     │
│  │  └─────────────┘    └─────────────┘    └─────────────┘       │     │
│  │         │                  │                  │              │     │
│  │         ▼                  │                  ▼              │     │
│  │  ┌─────────────┐           │           ┌─────────────┐       │     │
│  │  │ 事件监听    │           │           │   报告层    │       │     │
│  │  │agent/event  │           │           │agent/reporter      │     │
│  │  └─────────────┘           │           └─────────────┘       │     │
│  │                              │                  │              │     │
│  │                              ▼                  ▼              │     │
│  │                       ┌─────────────┐    ┌─────────────┐       │     │
│  │                       │   Redis     │    │   展示层    │       │     │
│  │                       │   数据库    │    │ agent/view  │       │     │
│  │                       └─────────────┘    └─────────────┘       │     │
│  │                                                               │     │
│  │  ┌─────────────────────────────────────────────────────────┐  │     │
│  │  │                    工具层 (utils/)                       │  │     │
│  │  │  timer.py │ wrapper.py │ config.py │ constants.py       │  │     │
│  │  └─────────────────────────────────────────────────────────┘  │     │
│  └───────────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         libvirt/QEMU/KVM 层                            │
│              ┌─────────┐    ┌─────────┐    ┌─────────┐                 │
│              │  VM 1   │    │  VM 2   │    │  VM N   │                 │
│              └─────────┘    └─────────┘    └─────────┘                 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 简化模块图

```
┌─────────────────────────────────────────────────────────┐
│                     VM Analyzer Agent                   │
├──────────────┬──────────────┬────────────┬──────────────┤
│   采集层     │   存储层     │   分析层   │   展示层     │
├──────────────┼──────────────┼────────────┼──────────────┤
│ gather/      │ agent/storage│ agent/     │ agent/view   │
│  采集各种    │  数据持久化  │ analyze    │  结果展示    │
│  指标数据    │  Redis存储   │  性能分析  │  控制台/文件 │
└──────────────┴──────────────┴────────────┴──────────────┘
```

## 模块详细说明

### 1. 采集层 (gather/)
负责采集虚拟机和宿主机的各项性能指标：
- **基础信息采集**：虚拟机名称、UUID、配置、运行状态等
- **CPU指标**：使用率、负载、vCPU调度信息、上下文切换等
- **内存指标**：使用率、交换分区使用、页错误、内存块信息等
- **磁盘指标**：使用率、IOPS、吞吐量、延迟、健康状态等
- **网络指标**：流量、丢包、错误数、接口信息等
- **进程指标**：TOP进程CPU/内存使用、进程数、线程数等
- **宿主机信息**：CPU、内存、磁盘、系统版本等

### 2. 核心层 (agent/)
#### 2.1 事件监听 (event.py)
- 基于libvirt事件机制监听虚拟机生命周期事件
- 支持虚拟机创建、启动、停止、暂停、恢复、销毁等事件
- 自动更新监控列表，无需重启服务

#### 2.2 虚拟机管理 (vm.py)
- 虚拟机对象工厂，统一管理所有虚拟机实例
- 提供虚拟机信息查询和操作接口
- 缓存虚拟机基本信息，减少重复查询

#### 2.3 采集调度 (collector.py)
- 定时调度采集任务，支持自定义采集间隔
- 多线程并行采集，提高采集效率
- 自动处理采集超时和错误

#### 2.4 存储层 (storage.py)
- Redis数据存储，支持高并发读写
- 自动过期机制，防止数据无限增长
- 结构化存储，便于后续分析和查询

#### 2.5 分析层 (analyze.py)
- 性能瓶颈分析，自动识别资源瓶颈
- 阈值告警，支持多级别告警配置
- 趋势分析，预测性能变化

#### 2.6 报告层 (reporter.py)
- 定期生成性能报告
- 支持多维度统计分析
- 可扩展多种报告输出方式

#### 2.7 展示层 (view.py)
- 控制台友好输出，便于人工查看
- 文件输出，支持JSON、CSV等格式
- 可扩展对接监控系统

### 3. 工具层 (utils/)
提供通用工具函数：
- **timer.py**：定时器工具，支持定时任务
- **wrapper.py**：通用装饰器（单例模式等）
- **constants.py**：常量定义
- **config.py**：配置管理
- **metrics_collector.py**：指标采集工具类
- **vm_states.py**：虚拟机状态常量

### 4. 诊断工具 (diagnose/)
提供虚拟机问题诊断工具：
- OOM检测工具
- QEMU性能分析工具
- 大页检测工具
- 虚拟化健康检查工具

### 5. 一键功能 (oneClickEnable/)
提供一键开启优化功能：
- vCPU绑定功能
- 热升级工具
- 性能监控事件监听

## 数据流
1. libvirt事件 → 事件监听模块 → 更新虚拟机列表
2. 定时器 → 采集器 → 调用采集脚本 → 采集指标数据
3. 采集到的数据 → 存储模块 → 写入Redis
4. 分析器定期从Redis读取数据 → 性能分析 → 生成告警和报告
5. 报告生成 → 展示层 → 输出到控制台/文件/外部系统

## 技术栈
- **语言**：Python 3.6+
- **虚拟化接口**：libvirt
- **数据存储**：Redis
- **并发模型**：多线程 + 事件驱动
- **日志**：Python logging模块
- **部署方式**：物理机部署、容器部署
